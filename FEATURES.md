# 特征字段说明(features.csv,30 项)

由 `features.py` / 网页「提取滑坡特征」生成,每张抓图一行。**流程与报警逻辑见 [PIPELINE.md](PIPELINE.md)**。

所有几何量都在 DA V2 重建坐标系下计算——形状比例正确,但存在仿射系统偏差(见文末)。
**监测看时间序列的相对变化,不要看绝对值。**

## 元信息

| 字段 | 含义 |
|---|---|
| `time` | 本帧时间(ISO)。命令行按文件名解析,网页端取当前时间 |
| `image` | 源图片文件名(仅命令行模式) |

## ① 图像质量(噪声门控)

| 字段 | 含义 | 范围 | 处理 |
|---|---|---|---|
| `img_brightness` | 平均亮度 | 0~1 | <0.12(夜间/雨雾)或 >0.95(过曝)→ 该帧不可信 |
| `img_blur` | 拉普拉斯方差(越大越清晰) | 一般 >40 | 太小=失焦/糊 → 该帧不可信 |

## ② 图像分割

| 字段 | 含义 | 危险方向 |
|---|---|---|
| `seg_landslide_frac` | 滑坡区域像素占比 | 上升=坡面失稳范围扩大 |
| `seg_landslide_max` | 最大滑坡连通域占比 | 上升=出现大面积连续失稳 |
| `seg_deep valley_frac` | 深沟/谷地占比 | 反映地形基准,突变说明坡形改变 |
| `seg_deep valley_max` | 最大深沟连通域占比 | 同上 |
| `seg_person_n` | 行人实例数 | 施工活动强度(其区域已从几何特征剔除) |
| `seg_car_n` | 轿车数 | 同上 |
| `seg_truck_n` | 卡车数 | 同上 |
| `seg_construction vehicle_n` | 工程车数 | 同上 |

> **动态物体处理**:person/car/truck/construction vehicle 的像素掩码会从深度统计、
> 点云几何和帧间差异中**全部排除**,避免人和车造成的假变化。数量仍保留作为背景信息。

## ③ 深度分布(归一化逆深度/视差,值越大越近)

| 字段 | 含义 | 范围 | 危险方向 |
|---|---|---|---|
| `disp_p05` | 视差 5% 分位(最远处) | 0~1 | 下降=画面中远处占比变大(坡体后退) |
| `disp_p50` | 视差中位数 | 0~1 | 双向变化都值得关注 |
| `disp_p95` | 视差 95% 分位(最近处) | 0~1 | 上升=有物体逼近(堆积体前缘) |
| `disp_std` | 视差标准差 | 0~0.5 | 骤降可能是深度模型失效 |

## ④ 三维几何(点云局部法向量)

| 字段 | 含义 | 单位 | 危险方向 |
|---|---|---|---|
| `slope_mean` | 坡度角均值 | 度 0~90 | 持续增大=坡面变陡 |
| `slope_p95` | 坡度 95% 分位 | 度 | 陡坎增多会上升 |
| `rough_local` | 局部粗糙度(坡度在 5×5 窗口的标准差) | 度 | 增大=表面破碎化 |
| `curv_mean` | 曲率均值 | 0~1 | 增大=局部凹凸加剧 |
| `plane_tilt` | PCA 主平面倾角 | 度 | 增大=整体坡面变陡 |
| `plane_rms` | 相对主平面的归一化残差 | 0~1 | 增大=表面解体/不平整 |
| `plane_skew` | 主平面上下偏斜度(法向固定朝上) | 无量纲 | 绝对值增大=异常;正=隆起堆积,负=塌陷缺失 |
| `bulge_frac` | 凸起面积占比 | 0~1 | **上升是坡脚鼓胀的经典前兆** |
| `edge_depth_corr` | 图像边缘与深度边缘相关性 | -1~1 | 接近 0 = 该帧深度不可信,应降权 |

## ⑤ 帧间时序(与上一帧配准后)

| 字段 | 含义 | 范围 | 危险方向 |
|---|---|---|---|
| `shift_px` | 配准位移模长 | 像素 | >8 = 相机被碰动/漂移,本身即告警 |
| `shift_resp` | 配准响应值 | 0~1 | <0.2 该帧变化特征不可信 |
| `diff_mean` | 深度差均值 | 0~1 | 增大=整体深度结构变化 |
| `diff_p95` | 深度差 95% 分位 | 0~1 | 增大=局部剧烈变化 |
| `diff_frac` | 深度变化超 10% 的面积占比 | 0~1 | **形变核心指标**,持续上升=坡体活动 |

## ⑥ 降雨(仅命令行)

| 字段 | 含义 | 单位 |
|---|---|---|
| `rain_1h` / `rain_24h` / `rain_72h` | 抓图前累积降雨 | mm |

降雨是滑坡的主要**触发因素**,也是解释"雨后画面变化"的关键变量,建议务必接入。

---

## 已知偏差与使用建议

1. **仿射偏差**:DA V2 输出满足 `d = a/z + b`,我们只修正了缩放 `a`、忽略偏移 `b`,
   坡度/倾角**绝对值偏大且有系统误差**(实测 44~73°),越远越明显。同一相机下偏差恒定,
   **做时间序列差分会被抵消**,所以看变化量而非绝对值。
2. **无绝对尺度**:位移是像素单位;体积、裂缝宽度等米制量算不出,需放已知尺寸参照物标定。
3. **缺失值**:每段第一帧的帧间变化特征为 `nan`;未检出的类别为 0。
   喂 xLSTM 前建议用列中位数填充 `nan`,并保留缺失掩码列。
4. **降权策略**:`edge_depth_corr` 和 `shift_resp` 低的行说明该帧不可信,建议降权或剔除。
5. **无标签场景**:把 xLSTM 用作"特征向量下一步预测器",预测残差即异常分,不需要灾害标签。
