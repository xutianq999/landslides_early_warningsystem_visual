# 滑坡监测与预警流程(PIPELINE)

从监控视频流到报警的完整逻辑。四层数据源逐级加工,报警信号来自**时间序列的导数**。

```
海康相机(固定、变焦锁定)
   │  定时抓图 / 视频流抽样(如每 5 分钟一帧)
   ▼
┌──────────────────────────────────────────────────────────────┐
│ ⓪ 采样与质量门控                                              │
│   亮度 / 清晰度 / 配准响应 / 相机位移 → 不合格帧丢弃,不报警      │
│   (夜间、雨雾、失焦、相机被碰动的帧全部剔除)                    │
└──────────────────────────────────────────────────────────────┘
   ▼
┌──────────────────────────────────────────────────────────────┐
│ ① 图像层(RGB)                                                │
│   YOLOE 零样本分割:                                           │
│     deep valley / landslide → 滑坡区域面积、最大连通块          │
│     person / car / truck / construction vehicle → 动态物体掩码  │
└──────────────────────────────────────────────────────────────┘
   ▼ 动态物体掩码
┌──────────────────────────────────────────────────────────────┐
│ ② 深度层(DA V2)                                              │
│   相对深度分布(视差分位数);动态物体区域排除                    │
└──────────────────────────────────────────────────────────────┘
   ▼
┌──────────────────────────────────────────────────────────────┐
│ ③ 点云层(反投影)                                              │
│   坡度 / 粗糙度 / 曲率 / 主平面 / 坡脚鼓胀;动态物体区域排除     │
└──────────────────────────────────────────────────────────────┘
   ▼
┌──────────────────────────────────────────────────────────────┐
│ ④ 时序层(帧间)                                                │
│   与上一帧配准 → 深度差 → 变化率(一阶差分) → 加速度(二阶差分)   │
└──────────────────────────────────────────────────────────────┘
   ▼
features.csv(每帧一行,30 项特征)
   ├─▶ alarm.py   实时分级报警(规则法,立即可用)
   └─▶ xLSTM      特征向量下一步预测,残差即异常分(需积累数据)
```

---

## 一、四层数据源详解

| 层 | 数据 | 计算方式 | 产出特征 |
|---|---|---|---|
| ① 图像 | 原始 RGB | YOLOE 零样本分割 | `seg_landslide_frac/_max`、`seg_deep valley_frac/_max`、动态物体计数 `seg_*_n` |
| ② 深度 | DA V2 相对深度 | 归一化逆深度(视差)统计 | `disp_p05/p50/p95/std` |
| ③ 点云 | 深度反投影 | 局部法向量、PCA 主平面 | `slope_mean/p95`、`rough_local`、`curv_mean`、`plane_tilt/rms/skew`、`bulge_frac` |
| ④ 时序 | 前后帧配准 | 相位相关 + 深度差 | `diff_mean/p95/frac`、`shift_px`、`shift_resp` |

**为什么要有四层**:图像层看"表面有没有变",深度层看"远近结构有没有变",点云层看"三维形状有没有变",时序层看"变得有多快"。单看任何一层都容易误判。

---

## 二、动态物体剔除(防人为误差)

人和车经过会造成两类假象:遮挡滑坡区域(面积骤降)、局部深度突变(差异飙升)。

处理:分割得到 `person/car/truck/construction vehicle` 的像素掩码,**并集**后作为忽略区,
在深度统计、点云几何、帧间差异三处全部排除。动态物体的**数量**仍保留为特征
(`seg_*_n`),因为施工活动本身是有价值的背景信息。

> 注意:掩码会有边缘残留(分割边界不精确)。所以还要靠后面的中值滤波 + 持续性判定兜底。

---

## 三、噪声抑制(四道防线)

| 防线 | 手段 | 对付什么 |
|---|---|---|
| 1. 质量门控 | 亮度、拉普拉斯方差、`shift_resp`、`shift_px` 阈值 | 夜间/雨雾/失焦/相机被碰动 |
| 2. 动态掩码 | 分割掩码排除 | 人车路过 |
| 3. 中值滤波 | 3 点滚动中位数 | 单帧尖刺(落叶、飞鸟、掩码残留) |
| 4. 持续性判定 | 连续 N 个采样点超阈值才报警 | 偶发抖动、单帧异常 |

四道防线缺一不可。实测:单帧噪声尖刺和不可信帧都能被正确压到"正常"。

---

## 四、报警判定(alarm.py)

每个监控信号独立算异常度 `a`(0~10):

```
a = 0.5·clip(持续偏离, 0, 5)      ← 相对固定参考期的偏离(慢速蠕变)
  + 1.0·clip(变化率,   0, 5)      ← 一阶差分(加速变形)
  + 0.5·clip(加速度,   0, 5)      ← 二阶差分(突变)
```

- **持续偏离**用**固定参考期**(最初若干采样点的中位数)作基线,所以慢速蠕变不会因为"滚动基线追上来"而消失
- **变化率/加速度**用**滚动中位数 + MAD** 作基线,抗离群
- 尺度设相对下限(信号幅度的 20%),防止基线恒定时微小抖动被放大成巨大 z 值

**等级 = 最严重信号的异常度**(不是平均,避免单个强信号被稀释):

| 异常度 | 等级 | 含义 |
|---|---|---|
| < 2.0 | 0 | 正常 |
| 2.0 ~ 4.0 | 1 | 黄色-关注 |
| 4.0 ~ 6.0 | 2 | 橙色-预警 |
| ≥ 6.0 | 3 | 红色-紧急 |

监控信号与权重:

| 信号 | 权重 | 方向 | 含义 |
|---|---|---|---|
| `diff_frac` | 3.0 | 升 | 深度变化面积占比(形变核心) |
| `bulge_frac` | 2.5 | 升 | 坡脚鼓胀 |
| `seg_landslide_frac` | 2.0 | 升 | 滑坡区域面积 |
| `plane_skew` | 1.5 | 双向 | 主平面偏斜(隆起/塌陷) |
| `rough_local` | 1.5 | 升 | 表面破碎化 |
| `slope_mean` | 1.0 | 升 | 坡度 |
| `plane_rms` | 1.0 | 升 | 平整度残差 |
| `disp_p50` | 1.0 | 双向 | 深度中位数(整体远近变化) |

**相机异常单独标记**:`shift_px` 超阈值 → `camera_alarm=True`。这不是滑坡,而是"相机被动过",必须人工确认,否则后续数据全部污染。

用法:

```bash
.venv/bin/python alarm.py features.csv                    # 输出 alarm_result.csv
.venv/bin/python alarm.py features.csv --window 48 --persist 3   # 按采样频率调窗口
.venv/bin/python alarm.py features.csv --t1 1.5 --t2 3 --t3 5    # 现场标定阈值
```

> **阈值必须现场标定**:先用现场"正常期"数据跑一遍,看各信号异常度的分布,
> 把阈值设在正常波动之上。默认值只是保守起点。

---

## 五、xLSTM 的角色(与规则法互补)

规则法(alarm.py)立即可用,但只能发现"已经发生"的形变。
xLSTM 用来**预测正常演变**,提前发现异常:

1. 输入:过去 T 个采样点的 30 维特征向量
2. 训练目标:预测下一个采样点的特征向量(自监督,**不需要灾害标签**)
3. 异常分 = 实际值 − 预测值(马氏距离或加权 L1)
4. 当残差持续超过基线 → 说明系统偏离了"正常模式",即使规则法还没报警

**降雨特征的作用**:下雨本来就会让画面变化,如果不把降雨喂给模型,雨后误报会很多。
降雨既是触发因素,也是解释变量。

两条路线并行:规则法负责"确定在动",xLSTM 负责"看起来不对劲"。

---

## 六、采样策略建议

| 场景 | 采样间隔 | 说明 |
|---|---|---|
| 日常监测 | 5~15 分钟 | 足够捕捉日尺度蠕变;数据量可控(一天 96~288 帧) |
| 降雨期加密 | 1~2 分钟 | 降雨是主要触发因素,加密采样 |
| 报警后 | 30 秒~1 分钟 | 确认趋势、判断是否加速 |

**不要用高帧率视频逐帧算**:相邻帧几乎无变化,算出来全是噪声,
而且 DA V2 每帧 100ms 级,24fps 根本跟不上。**低频抽样 + 帧间大间隔**才是正确用法。

---

## 七、当前局限

1. **无尺度标定**:位移是像素单位,米制量(体积、裂缝宽度)算不出;坡度有仿射系统偏差,看变化量不看绝对值
2. **单视角遮挡**:相机背后的坡体看不到;植被覆盖区深度不可靠
3. **裂缝检测缺失**:YOLOE 零样本对"裂缝"检出为 0,需另做边缘/线检测或微调模型
4. **阈值未标定**:默认阈值是保守起点,必须用现场正常期数据重新标定
5. **需要数据积累**:xLSTM 路线至少需要几百个采样点才能开始训练
